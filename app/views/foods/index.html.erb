<p id="notice"><%= notice %></p>
<head>
  <%= render "shared/navfood" %>
  <script type="text/javascript">
    $(document).ready(function() {
        $("#back2Top").click(function(event) {
          var mqq = window.matchMedia( "(max-width: 580px)" );
          var mq = window.matchMedia( "(min-width: 580px)" );
          var mmq = window.matchMedia( "(min-width: 770px)" );
          var m = window.matchMedia( "(min-width: 1000px)" );
          if (mq.matches) {
            if (mmq.matches) {
              if (m.matches) {
                $("html, body").animate({ scrollTop: 780 }, "med");
              return false;
              } else {
              $("html, body").animate({ scrollTop: 740 }, "med");
              return false;
              }
            } else {
              $("html, body").animate({ scrollTop: 660 }, "med");
            return false;
            }
          }
          else if (mqq.matches) {
            $("html, body").animate({ scrollTop: 620 }, "med");
            return false;
          }

          else {
             $("html, body").animate({ scrollTop: 8000 }, "med");
            return false;
          }
          
        });

    });
  </script>
</head>
<body>
  <br><br><br>
  <div class="container">
    <div class"card">
      <div class="comment">
        <div class="row">
          <div class="col-xsm-6">
            <div class="comments">
              <%= image_tag "Toulouse2.jpg" %>
            </div>
          </div>
          <div class="col-xsm-6">
            <div class="comment-text">
              <p>Hi, I love a good meal and I am pretty sure that your cat <br>thinks the same about that! Check out my favourites!</p>
            </div>
            <div class="check-it">
              <a id="back2Top"class="btn btn-primary" href _blanc>Check it out</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <br>
  <br>
</body>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= food.title %></td>
        <td><%= food.description %></td>
        <td><%= food.image %></td>
        <td><%= link_to 'Show', food %></td>
        <td><%= link_to 'Edit', edit_food_path(food) %></td>
        <td><%= link_to 'Destroy', food, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Food', new_food_path %>

  <%= render "shared/footer" %>
</body>